# Where the browser reaches the backend API. For local `npm run dev` this is
# the FastAPI server; in Docker compose it is set as a build arg + env var.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
